<template>
  <div @click="toDetailPage(product.id)" class="card-deck col-lg-3 col-md-6">
    <div class="card" style="width: 18rem; margin: 10px 15px;">
      <img :src="`${product.image_Url}`" class="card-img-top" alt="product" />
      <div class="card-body d-flex flex-column justify-content-between">
        <div>
          <h5 class="card-title">{{ product.name }}</h5>
        </div>
        <div id="foot" class="d-flex flex-row justify-content-between">
          <p class="card-text text-muted">Price: Rp.{{ product.price }}</p>
        </div>
      </div>
    </div>
  </div>
</template>

<script>
export default {
  name: 'Card',
  props: ['product'],
  methods: {
    toDetailPage: function (id) {
      this.$router.push(`/product/${id}`)
    }
  }
}
</script>

<style scoped>
.card-deck{
  /* margin-left: 5px;
  margin-right: 5px; */
  margin-bottom: 30px;
  padding: 0;
}

#foot{
  margin-top: 20px;
}

.edit{
  margin-right: 5px;
}

.card img{
  transition: transform .5s ease;
}

.card img:hover{
  transform: scale(1.05)
}
</style>
